server {
    listen 80;
    server_name pickom.qirelab.com;

    # Logging
    access_log /var/log/nginx/pickom-access.log;
    error_log /var/log/nginx/pickom-error.log;

    # Client size limit for file uploads
    client_max_body_size 50M;

    # Proxy settings
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;

    # Proxy to Next.js on localhost:3909
    location / {
        proxy_pass http://localhost:3909;
    }
}
