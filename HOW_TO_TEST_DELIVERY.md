# 🚀 Как протестировать функционал доставки

## 📝 Быстрый старт

### Шаг 1: Запустить backend сервер
```bash
cd pickom-server
npm run start:dev
```
Дождитесь сообщения: `Application is running on: http://localhost:3000`

### Шаг 2: Запустить frontend клиент
```bash
cd pickom-client
npm run dev
```
Дождитесь сообщения: `Ready on http://localhost:3001`

### Шаг 3: Открыть тестовый компонент
Перейдите в браузере: **http://localhost:3001/test-delivery**

---

## ✨ Что можно сделать в компоненте

### 1️⃣ Проверить информацию о пользователе
- Вверху страницы видно, авторизованы ли вы
- Если нет - перейдите на страницу авторизации

### 2️⃣ Загрузить список курьеров
- Нажмите кнопку **"📦 Загрузить курьеров"**
- Это проверяет `GET /delivery/pickers`
- Не требует авторизации
- Внизу появится список всех курьеров

### 3️⃣ Загрузить список доставок
- Нажмите кнопку **"🚚 Загрузить мои доставки"**
- Это проверяет `GET /delivery/requests`
- Требует авторизации
- Показывает только ваши доставки (по вашему UID)

### 4️⃣ Создать новую доставку
- Заполните форму:
  - **Название посылки** (обязательно)
  - **Описание** (опционально)
  - **Адрес откуда** (обязательно)
  - **Адрес куда** (обязательно)
  - **Цена** (обязательно)
  - **Размер** (маленький/средний/большой)
  - **Вес** (опционально)
  - **Заметки** (опционально)

- Нажмите **"✨ Создать доставку"**
- Это проверяет `POST /delivery/requests`
- Требует авторизации
- При успехе новая доставка появится в списке

---

## 🔍 Что происходит под капотом

### Когда вы нажимаете "Создать доставку":

1. **Frontend (React компонент)**
   ```typescript
   // Собирает данные из формы
   const formData = {
     title: "Тестовая посылка",
     fromAddress: "ул. Ленина 10, Минск",
     // ... и т.д.
   }

   // Получает Firebase токен
   const token = await session?.getIdToken();

   // Отправляет POST запрос
   fetch('http://localhost:3000/delivery/requests', {
     method: 'POST',
     headers: {
       'Authorization': `Bearer ${token}`,
       'Content-Type': 'application/json'
     },
     body: JSON.stringify(formData)
   })
   ```

2. **Backend (NestJS Controller)**
   ```typescript
   // Принимает запрос
   @Post('requests')
   @UseGuards(FirebaseAuthGuard)  // Проверяет токен
   async createDeliveryRequest(
     @Req() req: ReqWithUser,
     @Body() createDto: CreateDeliveryDto,  // Валидирует данные
   ) {
     const { uid } = req.user;  // Достаёт UID из токена
     return await this.deliveryService.createDeliveryRequest(uid, createDto);
   }
   ```

3. **Service (бизнес-логика)**
   ```typescript
   async createDeliveryRequest(senderId, createDto) {
     // Создаёт объект доставки
     const newRequest = {
       id: this.deliveryRequests.length + 1,
       senderId,
       ...createDto,
       status: 'pending',
       createdAt: new Date(),
     };

     // Сохраняет в массив (мок)
     this.deliveryRequests.push(newRequest);

     // Возвращает созданную доставку
     return newRequest;
   }
   ```

4. **Frontend получает ответ**
   ```typescript
   // Обновляет список доставок
   setDeliveries([newDelivery, ...deliveries]);

   // Показывает сообщение об успехе
   setSuccess(`Доставка #${newDelivery.id} создана успешно!`);
   ```

---

## 📊 Что отображается в компоненте

### Информационные блоки:
- ✅ **Зелёный блок** - успешная операция
- ❌ **Красный блок** - ошибка
- ℹ️ **Синий блок** - информация о пользователе

### Список курьеров (если загружен):
```
👤 Иван Курьеров
UID: 2
Роль: picker
Цена: 42.38 BYN
```

### Список доставок (если загружен):
```
#1 - Документы для визы
Статус: accepted (синий)
Откуда: ул. Ленина 10, Минск
Куда: пр. Победителей 5, Гродно
Цена: 100 BYN
Размер: small
Вес: 0.5 кг
Заметки: Позвонить за 10 минут
Создано: 17.01.2025, 12:00:00
```

---

## 🎨 Цвета статусов

- 🟠 **pending** - ожидает принятия курьером
- 🔵 **accepted** - курьер принял заказ
- 🟣 **picked_up** - курьер забрал посылку
- 🟢 **delivered** - доставлено
- 🔴 **cancelled** - отменено

---

## 🐛 Что делать при ошибках

### Ошибка: "Нет токена авторизации"
**Решение:** Войдите в систему через `/auth/login`

### Ошибка: "Failed to fetch"
**Решение:**
1. Убедитесь, что backend сервер запущен
2. Проверьте URL: `http://localhost:3000`
3. Проверьте CORS настройки в `main.ts`

### Ошибка: "Validation failed"
**Решение:**
1. Проверьте обязательные поля (title, fromAddress, toAddress, price, size)
2. Убедитесь, что size один из: 'small', 'medium', 'large'
3. Проверьте, что price >= 0

### Ошибка 401 Unauthorized
**Решение:**
1. Ваш токен истёк - перелогиньтесь
2. FirebaseAuthGuard настроен неправильно

---

## 🔍 Отладка

### Откройте DevTools (F12)

**Console вкладка:**
- Видно все логи запросов
- При создании доставки: `✅ Доставка создана: {id: 4, ...}`
- При ошибках: полный stack trace

**Network вкладка:**
- Видно все HTTP запросы
- Можно посмотреть Request/Response
- Проверить headers (особенно Authorization)
- Посмотреть payload (что отправили)

---

## 📱 Структура компонента (для понимания)

```typescript
// 1. STATE - данные компонента
const [deliveries, setDeliveries] = useState([]);
const [formData, setFormData] = useState({...});

// 2. API ФУНКЦИИ - запросы к серверу
const fetchPickers = async () => { ... }
const fetchDeliveries = async () => { ... }
const createDelivery = async () => { ... }

// 3. ОБРАБОТЧИКИ - реакция на действия пользователя
const handleInputChange = (e) => { ... }

// 4. ЭФФЕКТЫ - автоматическая загрузка
useEffect(() => {
  fetchPickers();  // Загружаем курьеров сразу
}, []);

// 5. JSX - разметка (HTML)
return <div>...</div>
```

---

## ✅ Чек-лист тестирования

- [ ] Сервер запущен на localhost:3000
- [ ] Клиент запущен на localhost:3001
- [ ] Открыта страница /test-delivery
- [ ] Авторизация пройдена
- [ ] Курьеры загружаются (зелёное сообщение)
- [ ] Доставки загружаются (зелёное сообщение)
- [ ] Форма заполнена
- [ ] Новая доставка создаётся (зелёное сообщение)
- [ ] Новая доставка появляется в списке
- [ ] В консоли нет ошибок
- [ ] В Network вкладке статус 200/201

---

## 💡 Советы для новичков

### 1. Используйте DevTools
- F12 открывает инструменты разработчика
- Console показывает логи и ошибки
- Network показывает все запросы

### 2. Читайте сообщения об ошибках
- Они обычно точно говорят, что не так
- Ищите ключевые слова: "required", "invalid", "not found"

### 3. Проверяйте пошагово
- Сначала GET запросы (они проще)
- Потом POST запросы
- Смотрите, что приходит в ответ

### 4. Не бойтесь экспериментировать
- Меняйте данные в форме
- Пробуйте невалидные данные
- Смотрите, какие ошибки возвращаются

### 5. Изучайте код компонента
- Там МНОГО комментариев для новичков
- Каждая строка объяснена
- Попробуйте изменить что-то и посмотрите результат

---

Удачи в тестировании! 🚀
