# Тестирование Capacitor APK на реальном телефоне

## Краткий ответ: ДА, можешь собрать APK и протестировать на телефоне!

## Два режима работы

### Режим 1: Dev режим с Live Reload (РЕКОМЕНДУЕТСЯ)
В этом режиме APK подключается к твоему локальному dev серверу. Изменения в коде видны сразу после обновления страницы в приложении.

**Преимущества:**
- Быстрая разработка
- Hot reload
- Можно использовать dev tools

**Требования:**
- Телефон и компьютер в одной WiFi сети
- Next.js dev server запущен
### Режим 2: Production Build (для финального тестирования)
APK содержит весь код внутри, не требует подключения к серверу.

**Преимущества:**
- Полностью автономное приложение
- Работает без WiFi (после первого запуска)
- Реалистичный тест production версии

**Недостатки:**
- Нужно пересобирать APK при каждом изменении
- Долго (build + sync + APK build)

---

## Dev режим: Пошаговая инструкция

### Шаг 1: Узнай свой локальный IP

**Windows:**
```bash
ipconfig
```

Ищи строку **IPv4 Address** в разделе твоего WiFi адаптера:
```
Wireless LAN adapter Wi-Fi:
   IPv4 Address. . . . . . . . . . . : 192.168.1.100
```

Запомни этот IP (например: `192.168.1.100` или `192.168.0.105`)

### Шаг 2: Отредактируй `capacitor.config.ts`

Открой файл `pickom-client/capacitor.config.ts` и измени:

```typescript
const config: CapacitorConfig = {
  appId: 'pickom.io',
  appName: 'Pickom',
  webDir: 'out',

  server: {
    // ЗАМЕНИ НА СВОЙ IP ИЗ ШАГа 1!
    url: 'http://192.168.1.100:3000',  // ← ТУТ ТВОЙ IP!
    cleartext: true,
  },

  android: {
    allowMixedContent: true,
  },

  // ... остальное
};
```

**ВАЖНО:**
- `10.0.2.2` - только для эмулятора
- Для реального телефона нужен твой реальный локальный IP

### Шаг 3: Запусти Next.js dev server

```bash
cd pickom-client
npm run dev
```

Убедись, что сервер запущен на порту 3000.

### Шаг 4: Синхронизируй изменения в Android

```bash
npx cap sync android
```

Это скопирует конфигурацию в Android проект.

### Шаг 5: Открой проект в Android Studio

```bash
npx cap open android
```

Или вручную: File → Open → выбери папку `pickom-client/android`

### Шаг 6: Собери и запусти APK

**Вариант A: Запуск на подключенном телефоне через USB**

1. Включи "Режим разработчика" на телефоне:
   - Настройки → О телефоне → Тапни 7 раз на "Номер сборки"

2. Включи "Отладка по USB":
   - Настройки → Для разработчиков → Отладка по USB ✅

3. Подключи телефон к компьютеру через USB

4. В Android Studio:
   - Выбери свое устройство в верхней панели
   - Нажми зеленую кнопку ▶ (Run)

**Вариант B: Экспорт APK для установки вручную**

1. В Android Studio: Build → Build Bundle(s) / APK(s) → Build APK(s)

2. Дождись завершения (внизу появится уведомление)

3. Нажми "locate" чтобы найти APK файл:
   ```
   pickom-client/android/app/build/outputs/apk/debug/app-debug.apk
   ```

4. Перекинь APK на телефон (через USB, мессенджер, или облако)

5. На телефоне:
   - Разрешить установку из неизвестных источников (если попросит)
   - Открой APK файл и установи

### Шаг 7: Проверка работы

1. Убедись что телефон и компьютер в одной WiFi сети

2. Убедись что Next.js dev server запущен (`npm run dev`)

3. Открой приложение Pickom на телефоне

4. Приложение должно загрузить контент с `http://твой_ip:3000`

**Если не работает:**
- Проверь, что IP в `capacitor.config.ts` правильный
- Проверь, что оба устройства в одной WiFi
- Попробуй открыть `http://твой_ip:3000` в браузере телефона
- Проверь файрвол на компьютере (может блокировать порт 3000)

---

## Production режим: Пошаговая инструкция

Этот режим нужен когда хочешь протестировать финальную версию без зависимости от dev сервера.

### Шаг 1: Отключи dev server в конфиге

Открой `capacitor.config.ts` и **закомментируй** блок server:

```typescript
const config: CapacitorConfig = {
  appId: 'pickom.io',
  appName: 'Pickom',
  webDir: 'out',

  // server: {
  //   url: 'http://192.168.1.100:3000',
  //   cleartext: true,
  // },

  android: {
    allowMixedContent: true,
  },

  // ... остальное
};
```

### Шаг 2: Собери Next.js проект

```bash
cd pickom-client
npm run build
```

Это создаст статический экспорт в папке `out/`

### Шаг 3: Синхронизируй с Android

```bash
npx cap sync android
```

Это скопирует содержимое `out/` в Android проект.

### Шаг 4: Открой в Android Studio

```bash
npx cap open android
```

### Шаг 5: Собери APK

В Android Studio: Build → Build Bundle(s) / APK(s) → Build APK(s)

APK будет здесь:
```
pickom-client/android/app/build/outputs/apk/debug/app-debug.apk
```

### Шаг 6: Установи на телефон

Перекинь APK на телефон и установи (как в Варианте B выше).

**Теперь приложение работает автономно!** Не нужен dev server, не нужен WiFi.

---

## Сравнение режимов

| Характеристика | Dev режим | Production режим |
|---------------|-----------|------------------|
| Скорость разработки | ⚡ Быстро | 🐌 Медленно |
| WiFi нужен | ✅ Да | ❌ Нет |
| Изменения видны сразу | ✅ Да | ❌ Нужен rebuild |
| Реалистичный тест | ⚠️ Частично | ✅ Да |
| Размер APK | 📦 Маленький | 📦 Больше |

---

## Быстрые команды

### Dev workflow:
```bash
# 1. Узнай IP
ipconfig

# 2. Измени capacitor.config.ts (укажи свой IP)

# 3. Запусти dev server
npm run dev

# 4. Sync и открой Android Studio
npx cap sync android && npx cap open android

# 5. Нажми Run в Android Studio
```

### Production workflow:
```bash
# 1. Закомментируй server.url в capacitor.config.ts

# 2. Build Next.js
npm run build

# 3. Sync и открой Android Studio
npx cap sync android && npx cap open android

# 4. Build APK в Android Studio
```

---

## Частые проблемы

### Проблема: "ERR_CONNECTION_REFUSED" в приложении
**Решение:**
- Проверь что dev server запущен
- Проверь IP в `capacitor.config.ts`
- Убедись что телефон и ПК в одной WiFi

### Проблема: Белый экран при запуске
**Решение:**
- Открой Chrome на ПК
- `chrome://inspect/#devices`
- Найди свое устройство и нажми "inspect"
- Смотри console на ошибки

### Проблема: "out/ directory not found"
**Решение:**
- Запусти `npm run build` перед sync
- Или используй dev режим с `server.url`

### Проблема: Изменения не видны в APK
**Решение:**
- В dev режиме: перезапусти dev server
- В production режиме: нужен новый `npm run build` + `npx cap sync`

---

## Рекомендация

**Для разработки:** Используй Dev режим с Live Reload

**Для финального теста:** Используй Production режим перед релизом

**Твой случай:** Если твой домен = твой IP, то dev режим идеален! Просто укажи свой локальный IP в `capacitor.config.ts` и всё заработает.

---

## Next Steps

1. Узнай свой IP (`ipconfig`)
2. Измени `capacitor.config.ts`
3. Запусти `npm run dev`
4. Собери APK
5. Тестируй на телефоне! 🚀
