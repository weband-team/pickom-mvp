# Задача: Исправление модальных окон, поддержки темы и навигации

**Task ID**: ui-2025-10-14-modals-theme-navigation-fixes
**Создано**: 2025-10-14
**Статус**: 🟡 В процессе

## 📋 Описание задачи

Комплексные улучшения UI/UX для Pickom MVP с фокусом на:
1. **Поведение модальных окон** - чтобы они выезжали снизу мобильного контейнера, а не всей страницы
2. **Поддержка темной темы** - исправить отсутствующую поддержку темы на нескольких страницах
3. **Улучшения навигации** - исправить логику редиректов и пользовательский опыт
4. **Значки на нижней навигации** - обеспечить отображение счетчиков уведомлений/чатов везде
5. **Улучшения макета** - исправить позиционирование контейнера и отступы

## 🎯 Цели и критерии успеха

### Страница выбора типа пользователя (`/user-type`)
- [x] Убрать аватар профиля из правого верхнего угла ✅ (компонент UserAvatar найден на строках 35-38)
- [ ] Добавить шаг подтверждения при выборе роли (показать кнопку "Далее" вместо немедленного редиректа)
- [ ] Исправить поддержку темной темы (фон, кнопки, иконки)

### Нижняя навигация
- [ ] Отображать счетчик значков чата на всех страницах
- [ ] Отображать счетчик значков уведомлений на всех страницах
- [ ] Обеспечить работу значков для ролей отправителя и перевозчика
- ✅ Поддержка значков уже реализована в компоненте BottomNavigation (строки 143-180)

### Модальные окна/Drawer компоненты
- [ ] Исправить PickerCardModal, чтобы выезжал снизу мобильного контейнера (не со дна страницы)
- [ ] Исправить EditPickerCardModal, чтобы выезжал снизу мобильного контейнера
- [ ] Конвертировать Dialog "Сделать предложение" в SwipeableDrawer для консистентности
  - `available-deliveries/page.tsx` (строки 740-796)
  - `delivery-details/[id]/page.tsx` (строки 621-667)
- [ ] Включить закрытие свайпом вниз через компонент Puller
- [ ] Убедиться, что модальные окна покрывают весь мобильный контейнер + нижнюю навигацию

### Страница деталей доставки
- [ ] Закрепить контейнер с кнопками действий внизу мобильного контейнера (не плавающий)
- ⚠️ Текущая реализация использует `position: absolute` на строке 482

### Поток аутентификации
- [ ] Изменить редирект "Создать новый аккаунт" в /login на /user-type (вместо /register)
  - Строка 210: `<Link href="/register" passHref>`
- [ ] Обновить /user-type для редиректа на /register после выбора роли
  - Строка 19: Сейчас редиректит напрямую на /register

### Исправления темной темы
- [ ] Исправить белый зазор между мобильным контейнером и нижней навигацией
- [ ] `/profile` - Исправить жестко заданный белый фон EditableDescription (строка 68: `backgroundColor: '#fafafa'`)
- [ ] `/profile` - Обновить градиент карточки заработка перевозчика для темной темы
- [ ] `/profile` - Вернуть карточку "Мои заработки" в представление роли отправителя
- [ ] `/login` - Применить тему к кнопкам, иконке и фону (строка 136: `backgroundColor: '#ffffff'`)
- [ ] `/user-type` - Применить тему к иконке, кнопкам и фону (строка 26: `backgroundColor: '#ffffff'`)
- [ ] `/register` - Применить тему к фону и компонентам
- [ ] `MobileContainer` - backgroundColor prop жестко задан на строке 37

### Анализ архитектуры
- [ ] Оценить структуру mobile-container + bottomnavigation для мобильной адаптации
- [ ] Предоставить рекомендации при необходимости улучшений

## 📁 Релевантные файлы

### Компоненты страниц
- `pickom-client/app/user-type/page.tsx` - Страница выбора типа пользователя
  - Строки 35-38: Компонент UserAvatar для удаления
  - Строка 19: Прямой редирект на /register
  - Строка 26: Жестко заданный backgroundColor
- `pickom-client/app/login/page.tsx` - Страница входа с редиректом
  - Строка 210: Ссылка на /register нужно изменить на /user-type
  - Строка 136: Жестко заданный backgroundColor
- `pickom-client/app/register/page.tsx` - Страница регистрации
- `pickom-client/app/profile/page.tsx` - Страница профиля с проблемами темы
- `pickom-client/app/delivery-details/[id]/page.tsx` - Детали доставки с позиционированием кнопок
  - Строки 621-667: Dialog для "Сделать предложение" (конвертировать в SwipeableDrawer)
  - Строки 480-492: Абсолютно позиционированные кнопки действий
- `pickom-client/app/available-deliveries/page.tsx` - Страница доставок перевозчика с модальными окнами
  - Строки 740-796: Dialog для "Сделать предложение" (конвертировать в SwipeableDrawer)

### Модальные окна/Drawer компоненты
- `pickom-client/components/picker/PickerCardModal.tsx` - Модальное окно просмотра карточки перевозчика
  - Строки 48-62: Конфигурация SwipeableDrawer
  - Строки 27-38: Компонент Puller уже реализован
- `pickom-client/components/picker/EditPickerCardModal.tsx` - Модальное окно редактирования карточки перевозчика
  - Строки 94-107: Конфигурация SwipeableDrawer
  - Строки 26-38: Компонент Puller уже реализован

### Компоненты макета
- `pickom-client/components/ui/layout/MobileContainer.tsx` - Основная обертка контейнера
  - Строки 28-50: Режим no-frame
  - Строка 37: Жестко заданный prop backgroundColor
- `pickom-client/components/common/BottomNavigation.tsx` - Нижняя навигация со значками
  - Строки 100-187: Структура компонента
  - Строки 143-180: Поддержка значков уже реализована
  - Props: `unreadChatsCount`, `activeOrdersCount`, `unreadNotificationsCount`

### Компоненты профиля
- `pickom-client/components/profile/EditableDescription.tsx` - Редактор поля "О себе"
  - Строка 68: Жестко заданный `backgroundColor: '#fafafa'`

### Система тем
- `pickom-client/theme/theme.ts` - Конфигурация темы MUI
  - Функция создания темы: createPickomTheme(mode)
  - Переопределения компонентов для MuiButton, MuiTextField и т.д.
- `pickom-client/theme/colors.ts` - Цветовая палитра для светлой/темной тем
  - Светлая тема: `colors.light.background.default`, `.paper`, `.elevated`
  - Темная тема: `colors.dark.background.default` (#000000), `.paper` (#1a1a1a), `.elevated` (#2a2a2a)
- `pickom-client/components/common/ThemeWrapper.tsx` - Провайдер темы
- `pickom-client/contexts/ThemeContext.tsx` - Контекст режима темы
  - Хук useThemeMode()
  - Функция toggleTheme()
  - Тема сохраняется в localStorage

## 🔍 Контекст и исследование

### Анализ текущей архитектуры

**Структура мобильного контейнера:**
```
Box (внешняя обертка)
  └─ MobileContainer (375x812px)
      └─ Контент страницы
  └─ BottomNavigation (position: absolute, bottom: 0)
```

Структура использует:
- `MobileContainer` с `showFrame={false}` для продакшена
- `BottomNavigation` позиционирован абсолютно внизу (строка 103: `position: 'absolute'`)
- Фиксированные мобильные размеры (375x812px) соответствуют размерам iPhone
- Расчет высоты: `height: shouldShowBottomNav ? 'calc(100vh - 70px)' : '100vh'`

**Плюсы:**
✅ Консистентный мобильный viewport на всех страницах
✅ Поддержка safe area для iOS устройств (paddingBottom: env(safe-area-inset-bottom))
✅ Фиксированные размеры делают макет предсказуемым
✅ Нижняя навигация всегда видима

**Минусы:**
⚠️ Модальные окна привязаны к странице вместо контейнера (вызывает текущую проблему)
⚠️ Белый зазор между контейнером и навигацией в темной теме
⚠️ Абсолютное позиционирование может вызывать конфликты z-index

### Ключевые зависимости

**UI Framework:**
- Material-UI (MUI) v5+ с ThemeProvider
- SwipeableDrawer для мобильных модальных окон
- Кастомная система тем со светлым/темным режимом

**Управление состоянием:**
- localStorage для роли пользователя, настроек перевозчика
- sessionStorage для потока регистрации
- React хуки для локального состояния

**Навигация:**
- Next.js App Router (useRouter, usePathname)
- Firebase Authentication с редиректами на основе роли

### Связанные компоненты

**Паттерн модального окна:**
```tsx
<SwipeableDrawer
  anchor="bottom"
  open={open}
  onClose={onClose}
  onOpen={() => {}}
  disableSwipeToOpen
  PaperProps={{
    sx: {
      maxHeight: '90vh',
      borderTopLeftRadius: 16,
      borderTopRightRadius: 16,
      backgroundColor: 'background.paper',  // ✅ Уже использует тему!
    },
  }}
>
  <Puller />
  {/* Контент */}
</SwipeableDrawer>
```

**Компонент Puller (для жеста свайпа вниз):**
```tsx
const Puller = () => (
  <Box
    sx={{
      width: 40,
      height: 4,
      backgroundColor: 'action.disabled',  // ✅ Уже использует тему!
      borderRadius: 2,
      position: 'absolute',
      top: 8,
      left: 'calc(50% - 20px)',
    }}
  />
);
```

**Проблема:** Модальные окна рендерятся вне MobileContainer, поэтому выезжают со дна всей страницы.
**Варианты решения:**
1. Использовать Portal с prop container для ограничения модальных окон мобильным контейнером
2. Добавить prop `container` в SwipeableDrawer, указывающий на ref MobileContainer
3. Скорректировать z-index и стратегию позиционирования

### Архитектура системы тем

**Цветовая палитра:**
- Primary: #FF9500 (Оранжевый)
- Avatar: #6366F1 (Индиго)
- Светлая тема: белый фон, темный текст
- Темная тема: #000000 default, #1a1a1a paper, #2a2a2a elevated

**Текущие проблемы темы:**
1. Жестко заданные цвета обходят систему тем
2. `backgroundColor: '#ffffff'` на многих страницах (login, user-type, available-deliveries)
3. `backgroundColor: '#fafafa'` в EditableDescription
4. Prop `backgroundColor` в MobileContainer по умолчанию '#ffffff'

**Решение:** Использовать свойства, поддерживающие тему:
- `bgcolor: 'background.default'` - Фоны страниц
- `bgcolor: 'background.paper'` - Поверхности карточек
- `bgcolor: 'background.elevated'` - Приподнятые поверхности (EditableDescription)

### Реализация значков нижней навигации

Текущая реализация (BottomNavigation.tsx):
```tsx
interface BottomNavigationProps {
  className?: string;
  unreadChatsCount?: number;
  activeOrdersCount?: number;
  unreadNotificationsCount?: number;
  userRole?: 'picker' | 'sender';
}
```

**Компоненты значков уже реализованы:**
- Значок чата (строки 146-159)
- Значок уведомлений (строки 165-178)
- Значок заказов (строки 127-140)

**Отсутствует:** Страницы не передают props счетчиков значков компоненту BottomNavigation.

**Решение:**
1. Создать общий контекст/хук для счетчиков значков
2. Получать из API: `/api/notifications` и `/api/chats`
3. Передавать props со всех страниц, использующих BottomNavigation

## 📝 План реализации

### Фаза 1: Исправление поведения модальных окон
1. **Рефакторинг стратегии рендеринга модальных окон**
   - Протестировать Portal с prop container
   - Добавить ref в MobileContainer
   - Передать ref контейнера компонентам SwipeableDrawer

2. **Обновить компонент PickerCardModal**
   - Добавить prop container при необходимости
   - Проверить работу жеста свайпа вниз с Puller
   - Протестировать на десктопе и мобильном viewport

3. **Обновить компонент EditPickerCardModal**
   - Применить те же исправления, что и для PickerCardModal
   - Обеспечить консистентное поведение

4. **Конвертировать "Сделать предложение" из Dialog в SwipeableDrawer**
   - В `available-deliveries/page.tsx` (строки 740-796)
   - В `delivery-details/[id]/page.tsx` (строки 621-667)
   - Соответствовать стилю и поведению других модальных окон
   - Добавить компонент Puller
   - Использовать те же PaperProps стили

### Фаза 2: Исправление потока аутентификации
1. **Обновить редирект страницы входа**
   - Изменить строку 210: `<Link href="/user-type" passHref>`
   - Протестировать поток: /login → /user-type

2. **Обновить страницу user-type**
   - Удалить компонент UserAvatar (строки 35-38)
   - Добавить состояние для выбранной роли (useState)
   - Показать выбор роли → кнопку "Далее" (не немедленный редирект)
   - При клике на "Далее" → редирект на /register
   - sessionStorage уже корректно сохраняет роль

3. **Протестировать поток регистрации**
   - /login → "Создать новый аккаунт" → /user-type
   - Выбрать роль → "Далее" → /register
   - Завершить регистрацию → домашняя страница на основе роли
   - Проверить, что sessionStorage сохраняет выбранную роль

### Фаза 3: Поддержка темной темы
1. **Исправить зазор между контейнером и навигацией**
   - Обновить MobileContainer или родительский Box для использования `bgcolor: 'background.default'`
   - Обеспечить консистентный цвет фона в темном режиме

2. **Обновить фоны на уровне страниц**
   - Заменить `backgroundColor: '#ffffff'` на `bgcolor: 'background.default'`
   - Страницы для исправления:
     - login/page.tsx (строка 136)
     - user-type/page.tsx (строка 26)
     - register/page.tsx (найти и заменить)
     - available-deliveries/page.tsx (строка 352)
     - delivery-details/[id]/page.tsx (строка 321)

3. **Исправить prop backgroundColor в MobileContainer**
   - Строка 22: Изменить значение по умолчанию с `'#ffffff'` на использование темы
   - Обновить тип prop для принятия токенов темы или hex
   - ИЛИ: Удалить prop и всегда использовать тему

4. **Исправить компонент EditableDescription**
   - Заменить строку 68: `backgroundColor: '#fafafa'`
   - Использовать `bgcolor: 'background.elevated'`

5. **Обновить поддержку темы страницы входа**
   - Внешний Box: Использовать `bgcolor: 'background.default'`
   - Проверить, что PickomLogo учитывает тему
   - Кнопки уже используют тему через компоненты

6. **Обновить поддержку темы страницы user-type**
   - Внешний Box: Использовать `bgcolor: 'background.default'`
   - Состояния hover кнопок: Использовать цвета темы
   - Строки 69-72: Заменить жестко заданные цвета на тему

7. **Обновить поддержку темы страницы регистрации**
   - Найти и заменить жестко заданные фоны
   - Использовать `bgcolor: 'background.default'`

8. **Обновить карточку заработка перевозчика на странице профиля**
   - Создать градиент, поддерживающий тему
   - Использовать условные стили на основе режима темы
   - Рассмотреть возврат "Мои заработки" роли отправителя (нужно уточнение у пользователя)

### Фаза 4: Значки нижней навигации
1. **Создать провайдер/контекст данных значков**
   - Создать `contexts/BadgeContext.tsx`
   - Получать количество непрочитанных чатов из API
   - Получать количество непрочитанных уведомлений из API
   - Делиться данными между всеми страницами
   - Использовать polling или websockets для обновлений в реальном времени

2. **Обновить компонент BottomNavigation**
   - Компонент уже имеет поддержку значков (строки 143-180)
   - Изменения в самом компоненте не требуются

3. **Обновить все страницы для передачи счетчиков значков**
   - Использовать BadgeContext на каждой странице с BottomNavigation:
     - delivery-methods
     - available-deliveries (строка 719)
     - profile
     - chats
     - notifications
     - delivery-details
   - Передать props: `unreadChatsCount`, `unreadNotificationsCount`, `activeOrdersCount`

### Фаза 5: Позиционирование кнопок на странице деталей доставки
1. **Обновить контейнер кнопок действий**
   - Текущее: `position: absolute` с ручным смещением снизу (строки 480-492)
   - Новый подход: Использовать sticky позиционирование или flexbox
   - Обеспечить, чтобы кнопки оставались над BottomNavigation
   - Рассчитать нижний отступ: 70px (высота BottomNav) + padding
   - Протестировать поведение прокрутки

### Фаза 6: Обзор архитектуры и рекомендации
1. **Протестировать на различных viewport**
   - Десктопный браузер
   - Mobile Safari iOS
   - Chrome Android
   - Проверить корректную работу safe areas

2. **Задокументировать находки**
   - Перечислить плюсы/минусы текущего подхода
   - Предложить улучшения при необходимости
   - Рассмотреть использование CSS переменных для размеров
   - Оценить стратегию Portal для модальных окон

## 🧪 Инструкции по тестированию

### Ручное тестирование

#### Поведение модальных окон
- [ ] Открыть PickerCardModal - должен выезжать снизу мобильного контейнера
- [ ] Свайп вниз по серой ручке - должен закрывать модальное окно
- [ ] Открыть EditPickerCardModal - то же поведение
- [ ] Открыть "Сделать предложение" - должен быть drawer, не dialog
- [ ] Проверить, что модальные окна покрывают весь мобильный контейнер включая нижнюю навигацию
- [ ] Протестировать на десктопе (viewport 375px) и мобильном устройстве

#### Поддержка темы
- [ ] Переключить тему на каждой странице и проверить обновление всех элементов
- [ ] Проверить /login - кнопки, иконки, фон должны меняться
- [ ] Проверить /user-type - кнопки, иконки, фон должны меняться
- [ ] Проверить /register - все элементы должны меняться
- [ ] Проверить /profile - EditableDescription должен использовать цвета темы
- [ ] Проверить /profile - карточка заработка должна иметь подходящий градиент
- [ ] Проверить отсутствие белых зазоров в темном режиме между контейнером и навигацией

#### Поток навигации
- [ ] Начать с /login
- [ ] Кликнуть "Создать новый аккаунт" → должен перейти на /user-type
- [ ] Выбрать "Отправлять посылки" → должна появиться кнопка "Далее"
- [ ] Кликнуть "Далее" → должен перейти на /register с ролью в sessionStorage
- [ ] Завершить регистрацию → должен перейти на /delivery-methods
- [ ] Попробовать с ролью "Забирать и доставлять" → должен перейти на /available-deliveries

#### Нижняя навигация
- [ ] Создать тестовые чаты и уведомления
- [ ] Проверить появление значка на иконке чата на всех страницах
- [ ] Проверить появление значка на иконке уведомлений на всех страницах
- [ ] Проверить обновление значков в реальном времени
- [ ] Проверить работу значков для ролей отправителя и перевозчика

#### Детали доставки
- [ ] Открыть любую страницу деталей доставки
- [ ] Прокрутить вверх и вниз
- [ ] Проверить, что кнопки действий остаются закрепленными внизу
- [ ] Проверить, что кнопки не перекрываются с нижней навигацией
- [ ] Протестировать с различными статусами доставки

### Автоматизированное тестирование
- [ ] Добавить визуальные регрессионные тесты для переключения темы
- [ ] Добавить тесты взаимодействия для поведения модальных окон
- [ ] Протестировать поток навигации с Playwright/Cypress

## 🔖 Контрольные точки

### 🔖 Checkpoint: 2025-10-14 18:30

**Status**: ✅ Фаза 2 и Фаза 3 завершены | 🟡 В процессе

#### ✅ Completed

**Фаза 2: Поток аутентификации**
- ✅ Изменен редирект "Создать новый аккаунт" с `/register` на `/user-type` (`app/login/page.tsx:210`)
- ✅ Удален компонент UserAvatar из страницы `/user-type` (строки 35-38)
- ✅ Добавлен шаг подтверждения выбора роли с кнопкой "Next" (не немедленный редирект)
- ✅ Реализована визуальная обратная связь при выборе роли (`variant="contained"` для выбранной роли)
- ✅ Корректно работает sessionStorage для сохранения выбранной роли

**Фаза 3: Поддержка темной темы**
- ✅ **КОРНЕВОЕ ИСПРАВЛЕНИЕ**: Исправлен `MobileContainer.tsx` - удален hardcoded `backgroundColor = '#ffffff'` default prop
  - Изменен на `bgcolor: backgroundColor || 'background.default'` (строки 37, 71)
  - Теперь контейнер автоматически поддерживает темную тему
- ✅ Исправлены все страницы аутентификации:
  - `/login/page.tsx` - `bgcolor: 'background.default'` (строка 136)
  - `/user-type/page.tsx` - `bgcolor: 'background.default'` + theme-aware hover states
  - `/register/page.tsx` - `bgcolor: 'background.default'` (строка 296)
- ✅ Исправлена страница профиля (`/profile/page.tsx`):
  - Внешний Box: `bgcolor: 'background.default'`
  - Адаптивный градиент для My Earnings Card (темный/светлый режим)
  - Цвет кнопки View Earnings адаптируется к теме
- ✅ Исправлен `EditableDescription.tsx`:
  - TextField background: `bgcolor: 'background.elevated'` (строка 68)
- ✅ Исправлена страница чата (`/chat/[id]/ChatPageClient.tsx`):
  - Все состояния (loading, error, not found, main): `bgcolor: 'background.default'`
  - Header и message input: `borderColor: 'divider'`, `bgcolor: 'background.paper'`
  - Кнопка Send: использует `primary.main`, `primary.dark`, `action.disabled`
- ✅ Исправлена страница списка чатов (`/chats/page.tsx`):
  - Все состояния (loading, error, empty, main): `bgcolor: 'background.default'`
- ✅ Исправлены страницы доставок:
  - `/available-deliveries/page.tsx` - `bgcolor: 'background.default'`
  - `/delivery-details/[id]/page.tsx` - все состояния используют `bgcolor: 'background.default'`
- ✅ Исправлен белый промежуток между MobileContainer и BottomNavigation

#### 📝 Next Steps

**Фаза 1: Модальные окна** (следующий приоритет)
1. Исправить PickerCardModal - чтобы выезжал снизу мобильного контейнера
2. Исправить EditPickerCardModal - применить те же исправления
3. Конвертировать Dialog "Сделать предложение" в SwipeableDrawer:
   - `available-deliveries/page.tsx` (строки 740-796)
   - `delivery-details/[id]/page.tsx` (строки 621-667)

**Фаза 4: Значки нижней навигации**
1. Создать BadgeContext для счетчиков значков
2. Передать props на все страницы с BottomNavigation

**Фаза 5: Позиционирование кнопок**
1. Исправить абсолютное позиционирование кнопок в `/delivery-details/[id]`

**Фаза 6: Архитектурный обзор**
1. Протестировать на различных устройствах
2. Задокументировать рекомендации

#### 📂 Modified Files

```
M pickom-client/app/login/page.tsx
M pickom-client/app/user-type/page.tsx
M pickom-client/app/register/page.tsx
M pickom-client/app/profile/page.tsx
M pickom-client/app/chat/[id]/ChatPageClient.tsx
M pickom-client/app/chats/page.tsx
M pickom-client/app/available-deliveries/page.tsx
M pickom-client/app/delivery-details/[id]/page.tsx
M pickom-client/components/profile/EditableDescription.tsx
M pickom-client/components/ui/layout/MobileContainer.tsx
```

#### 💡 Notes

**Ключевое решение темной темы:**
- Проблема была не в отдельных страницах, а в самом компоненте `MobileContainer`
- Hardcoded `backgroundColor = '#ffffff'` default prop заставлял контейнер всегда быть белым
- Исправление в корне (MobileContainer) автоматически решило проблему для всех страниц

**Использованные theme tokens:**
- `background.default` - основной фон страниц
- `background.paper` - фон для карточек/панелей (header, message input)
- `background.elevated` - фон для приподнятых элементов (EditableDescription)
- `divider` - цвет разделителей (borders)
- `primary.main`, `primary.dark`, `primary.contrastText` - кнопки
- `action.hover`, `action.disabled`, `action.disabledBackground` - состояния действий
- `text.primary`, `text.secondary` - цвета текста

**Адаптивный градиент профиля:**
```tsx
background: theme.palette.mode === 'dark'
  ? 'linear-gradient(135deg, #4a5568 0%, #2d3748 100%)'
  : 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)'
```

**Тестирование:**
- Все страницы теперь корректно поддерживают переключение светлой/темной темы
- Белые промежутки устранены
- Градиенты адаптируются к режиму темы

## 📊 Рекомендация по архитектуре

**Текущая структура mobile-container + bottomnavigation ХОРОША для мобильной адаптации.**

**Обоснование:**
1. ✅ Фиксированные размеры соответствуют обычным мобильным viewport
2. ✅ Поддержка safe area для современных iOS устройств
3. ✅ Консистентный опыт на всех устройствах
4. ✅ Нижняя навигация всегда доступна
5. ✅ Встроенная обработка overflow

**Небольшие улучшения для рассмотрения:**
- Использовать CSS custom properties для размеров (проще настраивать)
- Рассмотреть добавление breakpoint для планшетов (другой max-width)
- Стратегия Portal для модальных окон для ограничения их контейнером
- Использовать React Context для передачи ref контейнера компонентам модальных окон

**Не требуется крупных архитектурных изменений** - текущая структура надежна для мобильно-ориентированного PWA.

## 🚀 Начало работы

1. Просмотреть этот документ онбординга
2. Запустить приложение локально: `cd pickom-client && npm run dev`
3. Протестировать переключение темы в DevTools браузера
4. Начать с Фазы 1 (Исправление поведения модальных окон)
5. Использовать команду /bookmark для отслеживания прогресса
6. Тестировать каждое изменение немедленно перед переходом дальше

## 📌 Заметки

- Всегда тестировать изменения темы в светлом и темном режимах
- Использовать DevTools браузера для симуляции мобильного viewport (375x812)
- Проверять вкладку Network для API вызовов при реализации значков
- Использовать React DevTools для инспектирования значений контекста темы
- Коммитить изменения часто с краткими сообщениями

---

### 🔖 Checkpoint: 2025-10-15 (Фаза 1 - Модальные окна завершена)

**Status**: ✅ Фаза 1 завершена | 🟡 В процессе

#### ✅ Completed

**Фаза 1: Исправление поведения модальных окон**
- ✅ Исправлен `PickerCardModal` - теперь выезжает снизу мобильного контейнера
  - Добавлен `useEffect` для нахождения mobile container
  - Добавлен `container` prop в SwipeableDrawer
  - Добавлен `position: 'absolute'` в PaperProps
  - Компонент теперь рендерится внутри MobileContainer, а не в document.body
- ✅ Исправлен `EditPickerCardModal` - применены те же исправления
  - Идентичная логика с container и positioning
  - Консистентное поведение с PickerCardModal
- ✅ Конвертирован Dialog "Make Offer" в SwipeableDrawer (`available-deliveries/page.tsx`)
  - Заменен Dialog на SwipeableDrawer с anchor="bottom"
  - Добавлен Puller компонент для swipe-down жеста
  - Добавлен header с Close кнопкой
  - Использованы те же PaperProps стили (borderRadius, maxHeight, backgroundColor)
  - Разделены секции: Header, Content, Actions
- ✅ Конвертирован Dialog "Make Offer" в SwipeableDrawer (`delivery-details/[id]/page.tsx`)
  - Полностью идентичная структура с available-deliveries
  - Консистентный UX для всех модальных окон

#### 🔧 Technical Implementation

**Решение проблемы модальных окон:**
1. **Проблема:** SwipeableDrawer по умолчанию создает Portal в `document.body`, поэтому модальное окно выезжало со дна всей страницы
2. **Решение:** Использование prop `container` для привязки модального окна к MobileContainer
3. **Ключевые изменения:**
   ```tsx
   const [container, setContainer] = useState<HTMLElement | null>(null);

   useEffect(() => {
     const mobileContainer = document.querySelector('[data-mobile-container]') as HTMLElement;
     setContainer(mobileContainer);
   }, []);

   <SwipeableDrawer
     container={container}
     ModalProps={{ keepMounted: false }}
     PaperProps={{
       sx: {
         maxHeight: '90vh',
         borderTopLeftRadius: 16,
         borderTopRightRadius: 16,
         backgroundColor: 'background.paper',
         position: 'absolute', // Ключевое свойство!
       },
     }}
   >
   ```

**Структура модальных окон:**
```
SwipeableDrawer
├─ Puller (серая ручка для свайпа)
├─ Header
│  ├─ Typography (Title)
│  └─ IconButton (Close)
├─ Content (px: 3, pb: 2, overflowY: 'auto')
│  └─ Form fields / Content
└─ Actions (px: 3, py: 2, borderTop, gap: 2)
   ├─ Button (Cancel - outlined)
   └─ Button (Submit - contained)
```

**Puller компонент:**
```tsx
const Puller = () => (
  <Box
    sx={{
      width: 40,
      height: 4,
      backgroundColor: 'action.disabled',
      borderRadius: 2,
      position: 'absolute',
      top: 8,
      left: 'calc(50% - 20px)',
    }}
  />
);
```

#### 📂 Modified Files

```
M pickom-client/components/picker/PickerCardModal.tsx
M pickom-client/components/picker/EditPickerCardModal.tsx
M pickom-client/app/available-deliveries/page.tsx
M pickom-client/app/delivery-details/[id]/page.tsx
```

#### 📝 Next Steps

**Фаза 4: Значки нижней навигации** (следующий приоритет)
1. Создать BadgeContext для счетчиков значков
2. Интегрировать с API для получения unread counts
3. Передать props на все страницы с BottomNavigation

**Фаза 5: Позиционирование кнопок**
1. Исправить абсолютное позиционирование кнопок в `/delivery-details/[id]`
2. Использовать sticky позиционирование или flexbox

**Фаза 6: Архитектурный обзор**
1. Протестировать все модальные окна на различных устройствах
2. Задокументировать финальные рекомендации

#### 💡 Notes

**Преимущества SwipeableDrawer над Dialog:**
- ✅ Более нативное мобильное поведение (выезжает снизу)
- ✅ Поддержка swipe-down жеста для закрытия
- ✅ Визуальный Puller индикатор для интуитивного взаимодействия
- ✅ Консистентность с другими мобильными приложениями
- ✅ Лучше подходит для мобильного viewport

**Тестирование:**
- Все модальные окна теперь выезжают снизу MobileContainer
- Swipe-down жест работает корректно
- Модальные окна покрывают весь viewport включая bottom navigation
- Поддержка темной темы (backgroundColor: 'background.paper')

**Важные детали:**
- `position: 'absolute'` в PaperProps критически важен для корректного позиционирования
- `container` prop должен указывать на элемент с `data-mobile-container` атрибутом
- `keepMounted: false` в ModalProps для лучшей производительности
- `disableSwipeToOpen` предотвращает случайное открытие модального окна
